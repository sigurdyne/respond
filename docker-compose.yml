services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: respond
      POSTGRES_USER: respond
      POSTGRES_PASSWORD: respond
    ports:
      - "5439:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
